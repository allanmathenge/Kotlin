package Loops

fun main(args: Array<String>){

    var index: Int = 7

    do {
        // Perform a set of actions while the value is not equal to 0.
        println("The value of index is $index")
        --index

    } while (index != 0)
}